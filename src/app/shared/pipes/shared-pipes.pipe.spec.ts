import { SharedPipesPipe } from './shared-pipes.pipe';

describe('SharedPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new SharedPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
